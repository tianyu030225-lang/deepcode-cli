# Deepcode CLI 需求文档

更新时间：2026-05-28

## 项目目标

把 Deepcode 做成一个 Linux 终端里的 AI 编程助手 CLI。用户拿到单个可执行文件后，直接运行即可进入安装流程；安装完成后，在终端输入 `deepcode` 启动 CLI。

整体体验参考 Claude Code 的终端交互，但品牌、配色、头像、命令和模型配置使用 Deepcode 自己的设计。界面以蓝色风格为主。

## 实现边界

项目采用 Go 外壳 + C 核心的实现方式：

- Go 负责 CLI 主循环、命令系统、会话组织、终端界面编排、HTTP/HTTPS 请求和 SSE 网络流读取。
- C 负责更贴近 Linux 系统编程课程的核心能力：文件读写删除、进程启动、管道捕获、进程组取消、终端授权菜单、工作区真实路径校验、JSON 构造和 JSON 解析。
- Go 和 C 源码必须分目录管理，不能继续混放在同一层目录。
- 网络流目前保留 Go 实现，不要求用 C socket/OpenSSL 重写。

## 安装与启动

- 构建产物是单个可执行文件。
- 首次运行构建产物时进入安装流程，并显示安装进度动画。
- 安装完成后，二进制复制到 `~/.local/bin/deepcode`。
- 如果 `~/.local/bin` 不在 `PATH` 中，安装器应尝试写入 shell 配置。
- 输入 `deepcode` 启动 CLI。
- 第一次启动展示启动动画，后续默认不展示。
- `deepcode -show` 可以再次展示启动动画。
- `deepcode -setting` 可以重新进入配置向导。

## 首次配置

首次启动后进入配置向导：

- 展示 Deepcode/DeepSeek 风格的蓝色启动动画。
- 动画结束后显示两个 provider 选项，支持上下键切换。
- 默认选项是 `DeepSeek API`。
- 第二个选项是 `自定义 API`。

DeepSeek API 配置：

- 用户填写 API Key。
- Base URL 自动使用默认值。
- 默认模型、轻量模型、最强模型自动配置。

自定义 API 配置：

- 用户填写 API Key。
- 用户填写 Base URL。
- 用户分别填写默认模型、轻量模型、最强模型。

回复风格配置：

- `简洁优先`：能短则短，不废话。
- `适中`：该详细的地方详细，该短的地方短。
- `详细`：宁可多说，不要漏重要信息。

## 主界面

- 配置完成后清屏并进入真正的 CLI 界面。
- 欢迎面板采用蓝色风格。
- 左侧显示 Deepcode 头像区域，头像素材由用户后续提供。
- 面板展示当前会话名、provider、model 和当前工作目录。
- 支持本地会话保存、恢复、重命名和上下文压缩。

## 命令

输入 `/` 时自动弹出命令列表。

需要支持的命令：

- `/压缩`：压缩当前线程上下文。
- `/子代理`：强制下一次任务启动子代理，子代理数量由模型决定。
- `/思考`：开关 DeepSeek thinking 模式；如果当前是自定义模型，需要提示“您使用的非 DeepSeek 模型，可能无法启动思考模式”。
- `/恢复`：查找并恢复历史对话。
- `/退出`：退出 CLI。
- `/危险`：下一次用户任务允许写文件、删除文件、执行 shell；只生效一次。
- `/very-danger`：隐藏命令，不出现在自动补全里；当前终端窗口持续获得危险权限。

当前实现还支持：

- `/rename` 或 `/重命名`：重命名当前会话。
- `/model` 或 `/模型`：切换默认、轻量、最强模型。
- `/show` 或 `/显示`：切换炫彩/简洁显示模式。
- `/show-thinking`：查看最近一次 thinking 内容。
- `/review` 或 `/审查`：审查当前工作目录代码快照。

## 权限模型

普通权限下，模型只能：

- 获取当前工作目录。
- 读取当前工作目录内的文件。

危险操作包括：

- 写文件。
- 删除文件。
- 执行 shell 命令。

危险操作必须经过授权：

- `/危险` 只对下一次用户任务生效，任务结束后撤销。
- `/very-danger` 只对当前终端窗口生效，不持久化到配置或会话文件。
- 工具路径必须限制在当前工作目录内，并且不能通过符号链接绕过。

## ESC 取消

CLI 必须支持用户主动停止正在进行的工作：

- 输入框中按裸 `Esc` 应立即取消当前输入，不应卡住等待后续转义序列。
- 模型流式回复时按 `Esc` 应停止本次请求。
- 已经显示在终端上的半截回复可以保留可见，但不写入会话历史。
- 取消后回到主输入。

## 子代理

子代理用于处理可拆分的并行任务。

当前要求：

- `/子代理` 只影响下一次用户任务。
- 下一次任务由模型拆分为 1 到 4 个子任务。
- 子代理并行执行。
- 子代理执行时，主进程必须等待，不允许用户继续正常聊天。
- 子代理执行时显示与主模型不同的专用动画。
- 等待期间按 `Esc` 应取消所有仍在运行的子代理。
- 所有子代理完成后，主进程自动把结果注入上下文，并由主模型生成最终答复。
- 子代理状态必须真实反映 running、done、failed、canceled，模型回答子代理状态问题时不能编造。

## 系统提示词要求

系统提示词必须保证：

- 默认使用中文回复。
- 技术术语、代码、路径、命令名和 API 名称可以保留英文。
- 普通 prose 不输出原始 ANSI 转义码。
- 需要工具时先简短说明。
- 工具被拒绝或取消时，用中文说明并尝试无需危险权限的替代方案。
- 如果用户询问当前 provider、model 或配置，必须按当前配置如实回答；不能确认就说不能确认。

原始外部 AI 生成的提示词中“不要暴露底层模型”的说法不合理，已调整为：不能编造或隐瞒，用户明确询问时应根据当前可见配置如实回答。

## 当前完成状态

已完成：

- Go 外壳和 C 核心分目录管理：`mycc/go` 和 `mycc/c`。
- 单文件安装流程。
- 首次配置向导。
- 蓝色启动动画和欢迎界面。
- slash 命令补全。
- 会话保存、恢复、重命名、上下文压缩。
- 工具调用和危险权限授权。
- 模型流式回复。
- ESC 取消输入和模型回复。
- 阻塞式并行子代理、子代理专用动画、Esc 取消全部子代理、完成后自动整合。
- 子代理状态注入模型上下文。
- 文件工具符号链接越界防护，真实路径校验由 C 实现。
- 子代理子进程捕获和取消由 C 的 fork/pipe/waitpid/kill 进程组实现。
- 模型请求 JSON、SSE chunk JSON、工具参数 JSON 由 C/cJSON 处理。

验证命令：

```bash
cd mycc
make test
make race
make vet
make
```
