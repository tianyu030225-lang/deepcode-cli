# 设计记录

这里记录 Deepcode 的模块分工和任务处理方式。构建、安装和命令用法见 [README](README.md)。

## Go 和 C 的分工

Go 源码在 `mycc/go`，负责：

- 主循环、命令处理和终端界面。
- 会话保存、恢复和上下文压缩。
- HTTP/HTTPS 请求，以及 SSE 流的读取。
- 模型工具调用和并行子任务的调度。

C 源码在 `mycc/c`，负责：

- 文件读写、删除和工作区路径校验。
- 进程启动、管道输出捕获、进程组取消。
- 终端授权菜单。
- 借助 cJSON 构造模型请求、解析 SSE 数据块和工具参数。

Go 通过 CGO 调用 C。网络传输仍由 Go 完成；C 解析收到的 JSON 数据，不负责建立 HTTP 连接。

## 安装和会话

构建产物是单个可执行文件。用户直接运行它时，程序复制自身到 `~/.local/bin/deepcode`；从安装位置运行时进入 CLI。

配置与会话分别放在 `~/.deepcode/config.json` 和 `~/.deepcode/sessions`，使用本地 JSON 文件。更新可执行文件时保留这些数据。

## 工具权限

读取工作区文件和获取当前目录不需要危险权限。写文件、删除文件和执行 shell 需要用户授权。

`/danger` 授权下一次任务，任务结束后撤销；`/very-danger` 持续到当前程序退出，不写入持久配置或历史会话。授权菜单等待用户选择，等待本身不算拒绝，按 `Esc` 则取消。

文件工具在实际操作时校验真实路径，防止符号链接绕过工作区限制。代码审查跳过符号链接和非普通文件。

shell 的边界不同：命令以当前用户权限执行，可以访问工作区以外的文件，也可以联网。设置工作目录不构成操作系统沙箱。

## 取消与进程

`Esc` 用于取消输入、模型请求和子代理任务。模型流中断后，已经显示的文字仍可留在终端，但不作为完整回复写入会话历史。

shell 执行接入任务的取消信号。调用结束时清理受管进程组；直接子进程退出后，输出管道最多再排空 1 秒，避免后台后代进程一直占用管道。因此不支持用该工具维持长期运行的服务。

## 并行子任务

`/agent` 为下一次用户任务开启子代理。模型将任务拆成 1–4 个子任务，子代理并行执行，主流程等待完成后汇总结果。

等待期间主界面不接受普通聊天输入，保留 `Esc` 取消。各子任务记录 running、done、failed 或 canceled 状态，结果与状态一同交回主模型。

子代理继承启动时的权限快照，运行期间不再弹出授权菜单。
